services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: ""
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: svc-applications
    ports:
      - "8003:5432"
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db:
